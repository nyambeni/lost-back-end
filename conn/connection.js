const mongosose = requere('mongoose');

//connect to mangodb
mongosose.connect('mongodb://localhost/testaroo');

mongosose.connection.once('open'.funtion(),
{
    
})
var MongoClient = require('mongodb').MongoClient;
//Create a database named "mydb":

